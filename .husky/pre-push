pnpm lint && pnpm typecheck

# Visual tests (skip with SKIP_VISUAL_TESTS=1)
if [ -z "$SKIP_VISUAL_TESTS" ]; then
  echo "Running visual tests... (skip with SKIP_VISUAL_TESTS=1)"
  pnpm --filter @ui-lib/css test:visual
fi
