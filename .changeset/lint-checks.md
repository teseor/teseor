---
"@teseor/css": patch
---

Add lint checks to prevent regressions: ban :focus (enforce :focus-visible), require id/title in docs.json, error on raw html in items, detect SCSS interpolation in api.json.
