# Changesets

Run `pnpm changeset` to record a release note for a `packages/` change.

`fixed: [["@teseor/*"]]` ties every `@teseor/*` package to a single synchronised version. See `docs/process/release.md` and `docs/ADR/0001-rewrite-not-migrate.md`.
