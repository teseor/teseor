---
"@teseor/css": minor
"@teseor/react": minor
"@teseor/vue": minor
---

Add `Center` layout primitive — centers a single child on both axes. Pure CSS, no children styling. `minHeight` is unset by default; pass `screen` (= 100dvh) for splash-screen / hero patterns, or override `--t-center-min-height` for custom values. Responsive prop supported.
