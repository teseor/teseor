---
---

Bump dev dependencies and clear two high-severity advisories (`ws` via happy-dom, `vite` `server.fs.deny` bypass via astro chain). No consumer-facing change; peer ranges on `@teseor/primitives`, `@teseor/react`, and `@teseor/vue` are unchanged.
