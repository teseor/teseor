---
"@teseor/css": minor
---

Split @layer tokens into tokens.core / tokens.scale / tokens.semantic for clearer DevTools hierarchy and theming. Font sizes now derive from --ui-font-size-base via calc().
