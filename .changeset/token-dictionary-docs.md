---
'@teseor/css': patch
---

Add token dictionary as single source of truth, replace ad-hoc banned-name regexes with dictionary-based lint, extract lint functions into modular files, add token architecture guide
