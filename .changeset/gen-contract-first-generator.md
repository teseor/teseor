---
---

First codegen generator. `pnpm gen contract <name>` reads `specs/<name>.yaml` and writes `packages/contract/src/<Name>.ts`. `@teseor/contract` is a new private workspace package; the published surface arrives when `gen-react` consumes it.
