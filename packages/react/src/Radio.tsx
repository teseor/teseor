"use client";

// AUTOGENERATED by gen-react. Do not edit.
// Source: specs/radio.yaml

import "@teseor/css/components/radio.css";
import type { ComponentProps, Ref } from "react";
import { mergeClass, type Responsive, responsiveDataAttrs } from "./_runtime.ts";

type RadioSize = "sm" | "md" | "lg";

type RadioOwnProps = {
  /** Size scale. */
  size?: Responsive<RadioSize>;
  children?: never;
  ref?: Ref<HTMLElementTagNameMap["input"]>;
};

export type RadioProps = Readonly<
  RadioOwnProps & Omit<ComponentProps<"input">, keyof RadioOwnProps>
>;

/**
 * Single radio button. Styled `<input type="radio">` with a painted dot and three sizes. Group exclusivity is consumer-owned via shared `name` (browsers handle it natively). Native `checked` / `defaultChecked` / `onChange` pass through `ComponentProps<"input">`. Form-association attributes (`name`, `form`, `required`, `disabled`) come from the formControl shared contract; HTML ignores `readOnly` on radio inputs.
 *
 * @example Default
 * ```tsx
 * <Radio />
 * ```
 *
 * @example Checked
 * ```tsx
 * <Radio defaultChecked />
 * ```
 *
 * @example Size Sm
 * ```tsx
 * <Radio size="sm" />
 * ```
 */
export function Radio(props: RadioProps) {
  const { size, children: _children, ref, className, ...rest } = props;

  const mergedClassName = mergeClass("t-radio", className);

  return (
    <input
      {...rest}
      ref={ref}
      className={mergedClassName}
      type="radio"
      {...responsiveDataAttrs("size", size)}
    />
  );
}
