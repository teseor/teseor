"use client";

// AUTOGENERATED by gen-react. Do not edit.
// Source: specs/pagination.yaml

import "@teseor/css/components/pagination.css";
import type { ComponentProps, Ref } from "react";
import { mergeClass } from "./_runtime.ts";

export type PaginationPageItem = {
  /** Stable item identity; required. */
  id: string;
  /** Page label rendered as the item text. */
  label?: string;
  /** Marks the active page; exposed as `data-current`. */
  current?: boolean;
};

type PaginationOwnProps = {
  /** Items rendered by the repeating `page` part. */
  pages?: ReadonlyArray<PaginationPageItem>;
  /** Forwarded ref to the wrapper element. */
  ref?: Ref<HTMLElementTagNameMap["nav"]>;
  /** List composites render only from their array prop — children are not accepted. */
  children?: never;
};

export type PaginationProps = Readonly<
  PaginationOwnProps & Omit<ComponentProps<"nav">, keyof PaginationOwnProps | "ref" | "children">
>;

/**
 * A static page-link list. Renders one item per entry in the `pages` array; navigation behavior is consumer-driven today.
 *
 * @example Three Pages
 * ```tsx
 * <Pagination pages={[{"id":"1","label":"1"},{"id":"2","label":"2","current":true},{"id":"3","label":"3"}]} />
 * ```
 *
 * @example Five Pages Last Active
 * ```tsx
 * <Pagination pages={[{"id":"1","label":"1"},{"id":"2","label":"2"},{"id":"3","label":"3"},{"id":"4","label":"4"},{"id":"5","label":"5","current":true}]} />
 * ```
 *
 * @example Empty
 * ```tsx
 * <Pagination pages={[]} />
 * ```
 */
export function Pagination(props: PaginationProps) {
  const { pages = [], className, ref, ...rest } = props;
  const mergedClassName = mergeClass("t-pagination", className);
  return (
    <nav {...rest} ref={ref} className={mergedClassName}>
      {pages.map((item) => (
        <span
          key={item.id}
          className="t-pagination-page"
          data-id={item.id}
          data-current={item.current || undefined}
        >
          {item.label}
        </span>
      ))}
    </nav>
  );
}
