"use client";

// AUTOGENERATED by gen-react. Do not edit.
// Source: specs/select.yaml

import "@teseor/css/components/select.css";
import type { ComponentProps, ReactNode, Ref } from "react";
import { mergeClass, type Responsive, responsiveDataAttrs } from "./_runtime.ts";

type SelectVariant = "outline" | "subtle" | "plain";

type SelectSize = "sm" | "md" | "lg";

type SelectOwnProps = {
  /** Visual style. */
  variant?: SelectVariant;
  /** Size scale. */
  size?: Responsive<SelectSize>;
  /** Marks the field as invalid for styling. Emits `data-invalid="true"` on the root. ARIA wiring (`aria-invalid`) is consumer-supplied or comes from the Field composite (tracked under #692). */
  invalid?: boolean;
  children?: ReactNode;
  ref?: Ref<HTMLElementTagNameMap["select"]>;
};

export type SelectProps = Readonly<
  SelectOwnProps & Omit<ComponentProps<"select">, keyof SelectOwnProps>
>;

/**
 * Native dropdown. Styled `<select>` with token-driven defaults and outline / subtle / plain visual variants. Children are consumer-supplied `<option>` and `<optgroup>` elements. The `multiple` HTML attribute renders the native listbox view and reaches the DOM via `ComponentProps<"select">` pass-through. Form-association attributes (`name`, `form`, `required`, `disabled`) come from the formControl shared contract; HTML ignores `readOnly` on select inputs.
 *
 * @example Default
 * ```tsx
 * <Select>Label</Select>
 * ```
 *
 * @example Variant Outline
 * ```tsx
 * <Select variant="outline">Label</Select>
 * ```
 *
 * @example Variant Subtle
 * ```tsx
 * <Select variant="subtle">Label</Select>
 * ```
 */
export function Select(props: SelectProps) {
  const { variant, size, invalid, children, ref, className, ...rest } = props;

  const mergedClassName = mergeClass("t-select", className);

  return (
    <select
      {...rest}
      ref={ref}
      className={mergedClassName}
      data-variant={variant}
      {...responsiveDataAttrs("size", size)}
      data-invalid={invalid === true ? "true" : undefined}
    >
      {children}
    </select>
  );
}
