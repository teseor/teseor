description: $auto Link component.

sections:
  variant:
    modifier: variant
    layout: row
    include_default: true
    description: $shared.variant
  disabled:
    modifier: disabled
    description: $shared.disabled
  external:
    modifier: external
    description: $auto External modifier.
