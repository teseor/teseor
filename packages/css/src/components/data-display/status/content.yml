description: $auto Status component.

sections:
  variant:
    modifier: variant
    layout: row
    include_default: true
    description: $shared.variant
  size:
    modifier: size
    layout: row
    description: $shared.size
  pulse:
    modifier: pulse
    description: $auto Pulse modifier.
