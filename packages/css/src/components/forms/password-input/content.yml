description: $auto Password Input component.

sections:
  size:
    modifier: size
    layout: row
    description: $shared.size
  state:
    modifier: state
    layout: row
    include_default: true
    description: $shared.state
  block:
    modifier: block
    description: $shared.block
  disabled:
    modifier: disabled
    description: $shared.disabled
