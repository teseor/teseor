<!-- AUTOGENERATED by gen-vue. Do not edit. -->
<!-- Source: specs/heading.yaml -->
<script setup lang="ts">
/**
 * Semantic prose heading. The rendered tag switches with `level` so outline tools and screen readers read native `<h1>`–`<h6>` structure.
 *
 * @example Default
 * ```vue
 * <Heading level="2">Label</Heading>
 * ```
 *
 * @example H1 Display
 * ```vue
 * <Heading level="1">Label</Heading>
 * ```
 *
 * @example H2 As Md Size
 * ```vue
 * <Heading level="2" size="md">Label</Heading>
 * ```
 */
import "@teseor/css/components/heading.css";
import { computed, type VNode } from "vue";
import { type Responsive, responsiveDataAttrs } from "./_runtime.ts";
import { Slot } from "./components/Slot.ts";

type HeadingSize = "xs" | "sm" | "md" | "lg" | "xl";

type HeadingLevel = "1" | "2" | "3" | "4" | "5" | "6";

type HeadingProps = {
  /** Size scale. */
  size?: Responsive<HeadingSize>;
  /** Outline level. Drives the rendered tag and the default visual size. */
  level?: HeadingLevel;
  /** Render directly on the consumer's child VNode via Slot (cloneVNode) instead of wrapping in a `<div>`. Single-child invariant. */
  asChild?: boolean;
};

const {
  size,
  level = "2",
  asChild,
} = defineProps<HeadingProps>();

defineSlots<{
  default?(): VNode[];
}>();

const tagMap = { "1": "h1", "2": "h2", "3": "h3", "4": "h4", "5": "h5", "6": "h6" } as const;

const attrs = computed(() => ({
  ...responsiveDataAttrs("size", size),
}));
</script>

<template>
  <component :is="asChild ? Slot : tagMap[level ?? '2']" class="t-heading" v-bind="attrs">
    <slot />
  </component>
</template>
