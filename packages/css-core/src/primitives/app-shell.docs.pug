//- Layout primitives documentation (app-shell, sidebar, main, container)
section#layout.ui-docs-section
  h2 Primitive: Layout
  p Layout primitives for app shells with sidebars and content containers.

  h3 App Shell + Sidebar + Main
  p This page uses the layout primitives:
  pre.ui-docs-code
    code.
      &lt;body class="ui-app-shell"&gt;
        &lt;aside class="ui-sidebar"&gt;
          &lt;!-- sidebar content --&gt;
        &lt;/aside&gt;
        &lt;main class="ui-main"&gt;
          &lt;div class="ui-container"&gt;
            &lt;!-- page content --&gt;
          &lt;/div&gt;
        &lt;/main&gt;
      &lt;/body&gt;

  h3 Sidebar Widths
  .ui-docs-example
    .ui-stack.ui-stack--sm
      .ui-cluster.ui-cluster--sm
        code .ui-sidebar--sm
        span 192px (24 units)
      .ui-cluster.ui-cluster--sm
        code .ui-sidebar--md
        span 240px (30 units) - default
      .ui-cluster.ui-cluster--sm
        code .ui-sidebar--lg
        span 320px (40 units)

  h3 Sidebar Position
  p By default, sidebar is on the start (left in LTR). Use
    code .ui-sidebar--end
    |  for right-side:
  pre.ui-docs-code
    code.
      &lt;aside class="ui-sidebar ui-sidebar--end"&gt;...&lt;/aside&gt;
      &lt;main class="ui-main ui-main--sidebar-end"&gt;...&lt;/main&gt;

  h3 Container Widths
  .ui-docs-example
    .ui-stack.ui-stack--sm
      .ui-cluster.ui-cluster--sm
        code .ui-container--sm
        span 640px (80 units)
      .ui-cluster.ui-cluster--sm
        code .ui-container--md
        span 800px (100 units)
      .ui-cluster.ui-cluster--sm
        code .ui-container--lg
        span 960px (120 units) - default
      .ui-cluster.ui-cluster--sm
        code .ui-container--xl
        span 1280px (160 units)
      .ui-cluster.ui-cluster--sm
        code .ui-container--full
        span No max width

  h3 Centered Container
  pre.ui-docs-code
    code.
      &lt;div class="ui-container ui-container--center"&gt;
        &lt;!-- centered content with max-width --&gt;
      &lt;/div&gt;
