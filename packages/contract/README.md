<!-- AUTOGENERATED by gen-contract. Do not edit. -->

# @teseor/contract

Framework-agnostic TypeScript type contracts for Teseor components. Generated from `specs/*.yaml`.

The React wrapper (`@teseor/react`) inlines its own type unions for self-containment; this package is the canonical type surface for consumers integrating Teseor in non-React TypeScript codebases (or in framework-agnostic tooling).

## Install

```
pnpm add @teseor/contract
```

## Usage

```ts
import type { ButtonProps } from "@teseor/contract/Button.ts";

function buildAttrs(props: ButtonProps): Record<string, string | undefined> {
  return { "data-variant": props.variant, "data-intent": props.intent };
}
```

## Types

- `Button` — `ButtonVariant`, `ButtonIntent`, `ButtonSize`, `ButtonProps`
- `Cluster` — `ClusterProps`
- `Code` — `CodeProps`
- `Codeblock` — `CodeblockProps`
- `Modal` — `ModalProps`
- `Pagination` — `PaginationProps`
- `Stack` — `StackProps`
- `Tablist` — `TablistProps`
- `Tooltip` — `TooltipProps`

## Generated content

Files in `src/` are autogenerated from `specs/*.yaml`. Do not edit them. The generator is `scripts/codegen/src/generators/gen-contract.ts`.
